# 快速启动指南

## 🚀 5 分钟快速开始

### 1. 安装依赖

```bash
npm install
```

> 注意：项目已配置 `.npmrc`，会自动使用 `--legacy-peer-deps` 以兼容 React 19

### 2. 配置 Supabase

#### 2.1 创建 Supabase 项目

1. 访问 [https://supabase.com](https://supabase.com)
2. 注册并创建新项目
3. 等待项目初始化完成（约 2 分钟）

#### 2.2 配置环境变量

编辑 `.env.local` 文件（已存在，只需修改）：

```env
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

获取这些值：
- 在 Supabase Dashboard → Settings → API 中找到
- `URL` 对应 `Project URL`
- `anon public` 对应 `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- `service_role` 对应 `SUPABASE_SERVICE_ROLE_KEY`

#### 2.3 初始化数据库

1. 在 Supabase Dashboard 中打开 SQL Editor
2. 复制 `supabase-schema.sql` 文件的全部内容
3. 粘贴并点击 Run 执行

这会创建：
- ✅ profiles 表
- ✅ RLS 安全策略
- ✅ 自动触发器
- ✅ 性能索引

### 3. 启动开发服务器

```bash
npm run dev
```

打开浏览器访问 [http://localhost:3000](http://localhost:3000)

---

## 🎯 测试功能

### 测试流程

1. **访问首页** → 看到欢迎页面和登录/注册按钮
2. **注册账号** → 填写邮箱、密码、选择角色
3. **邮箱验证** → 看到验证提示，10 秒倒计时
4. **登录账号** → 使用刚注册的账号登录
5. **查看页面**：
   - 普通用户 → `/profile` 个人信息页面
   - 作者 → `/dashboard` 管理后台

### 测试不同角色

#### 测试普通用户
```
注册时选择：普通用户
登录后跳转：/profile
可以查看：个人信息、账号状态
```

#### 测试作者
```
注册时选择：作者
登录后跳转：/dashboard
可以查看：统计数据、快速操作
```

---

## 📁 项目结构速览

```
/src
  /app
    /(auth)           认证页面
      /login          登录
      /register       注册
      /verify-email   邮箱验证
    /(protected)      受保护页面
      /profile        个人中心
      /dashboard      管理后台
    /api              统一 API
      /auth           认证接口
      /user           用户接口
    page.tsx          首页
    layout.tsx        布局
    globals.css       全局样式
  /components
    /ui               shadcn/ui 组件
    /auth             认证组件
  /lib
    /supabase         Supabase 配置
    utils.ts          工具函数
  /types              类型定义
  middleware.ts       路由中间件
```

---

## 🔐 认证流程说明

### 注册流程
1. 用户填写表单（邮箱 + 密码 + 角色）
2. 调用 `/api/auth/register`
3. Supabase 创建用户账号
4. 触发器自动创建 profile 记录
5. 跳转到邮箱验证页面
6. 10 秒倒计时后自动跳转登录

### 登录流程
1. 用户输入邮箱和密码
2. 调用 `/api/auth/login`
3. Supabase 验证凭证
4. 查询用户角色
5. 根据角色跳转：
   - `user` → `/profile`
   - `author` / `admin` → `/dashboard`

### 中间件保护
```typescript
// 自动处理
- 未登录访问受保护页面 → 跳转登录
- 已登录访问认证页面 → 跳转首页
- 普通用户访问 dashboard → 跳转 profile
```

---

## 🛠️ 常用命令

```bash
# 开发
npm run dev          # 启动开发服务器
npm run type-check   # TypeScript 类型检查
npm run lint         # ESLint 代码检查

# 构建
npm run build        # 构建生产版本
npm run start        # 运行生产服务器
```

---

## 🐛 常见问题

### Q: 安装依赖失败？
```bash
# 清除缓存重新安装
rm -rf node_modules package-lock.json
npm install
```

### Q: 登录后无法访问页面？
- 检查 Supabase 数据库是否执行了 `supabase-schema.sql`
- 确认 profiles 表已创建
- 查看浏览器控制台错误信息

### Q: 样式不显示？
- 确保 Tailwind CSS v4 依赖已安装
- 检查 `postcss.config.mjs` 配置
- 重启开发服务器

### Q: TypeScript 报错？
```bash
npm run type-check
```
查看具体错误信息

### Q: 邮件验证失败？
- Supabase 免费版每小时限制 3 封邮件
- 可以在 Supabase Dashboard → Authentication → Users 中手动验证邮箱
- 或配置自定义 SMTP 服务器

---

## 📚 进一步学习

### 文档资源
- [Next.js 文档](https://nextjs.org/docs) - App Router 和 API Routes
- [Supabase 文档](https://supabase.com/docs) - 认证和数据库
- [Tailwind CSS 文档](https://tailwindcss.com/docs) - 样式系统
- [shadcn/ui 文档](https://ui.shadcn.com) - UI 组件

### 项目文档
- `README.md` - 项目概述和功能介绍
- `SETUP.md` - 详细配置指南
- `API.md` - API 接口文档
- `supabase-schema.sql` - 数据库结构

---

## 🎉 下一步

项目已经实现了基础的 MVP 功能，后续可以：

- [ ] 添加忘记密码功能
- [ ] 实现个人资料编辑
- [ ] 添加头像上传
- [ ] 开发文章管理系统
- [ ] 集成数据统计
- [ ] 开发小程序端
- [ ] 开发移动 APP

---

## 💬 获取帮助

如遇问题，请查看：
1. 项目的 `README.md` 和 `SETUP.md`
2. `API.md` 中的接口文档
3. Supabase Dashboard 的日志
4. 浏览器开发者工具的控制台

祝开发顺利！🚀
